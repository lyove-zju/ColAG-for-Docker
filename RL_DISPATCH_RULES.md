# UAV RL 调度改造规则

本文档是后续所有 UAV RL 调度相关改动的硬约束。若实现细节与本文档冲突，以本文档为准。

## 1. 总体边界

- 本次改造只允许新增或修改 `Air_ws` 下的 UAV 调度模块。
- 本次改造的目标是：新增一套可切换的 UAV 侧 RL 调度方案，用来替换当前 VRPTW 在 UAV 调度中的作用。
- 原有 VRPTW 方案必须完整保留，且默认行为仍然是 VRPTW。
- RL 方案必须是可替换模块，用户可以通过参数在 `vrptw` 与 `rl` 之间切换。

## 2. 明确禁止项

以下内容禁止为了 RL 调度而被修改、删除或重写：

- UGV 的规划算法
- UGV 的碰撞检测逻辑
- UAV 和 UGV 的地图共享逻辑
- UAV 和 UGV 之间的 bridge / topic 通信逻辑
- `blind_info` 消息定义
- Ground 侧代码中的运行逻辑
- MARSIM 地图与仿真侧逻辑

如果未来确实需要修改以上内容，必须先更新本文档并明确说明原因，不能隐式突破边界。

## 3. VRPTW 基线保留要求

- `Air_ws/src/swarm_support/scripts/guide_plan.py` 是当前 VRPTW 基线实现，必须保留。
- 不允许删除、覆盖、重命名或用 RL 逻辑直接替换该文件。
- 当系统参数未显式启用 RL 时，运行结果必须保持为当前 VRPTW 路径。
- 如果 RL 效果不佳，必须能够仅通过参数切换回 VRPTW，而不需要回滚代码。

## 4. RL 模块接口约束

- RL 模块只能替换“调度顺序生成”这一环。
- RL 不负责轨迹避障，不负责地图建图，不负责控制器输出。
- RL 运行时输入边界必须与现有调度边界一致：
  - 输入：`blind_info`、`self_odom`
  - 输出：`guide_waypoint`
- RL 输出必须继续使用与 VRPTW 相同形状的 `nav_msgs/Path`，发布到 `guide_waypoint`。
- RL 模块不得要求 Ground 侧额外发布新 topic 才能工作。
- RL 模块若需要模型文件、训练脚本或离线数据，必须作为新增内容存在，不能破坏现有运行链。

## 5. 默认行为与错误处理

- 默认调度方式必须仍然是 `vrptw`。
- 只有在显式传入 `dispatch_method=rl` 时，才允许启动 RL 调度节点。
- 若 `dispatch_method=rl` 但模型路径无效或模型无法加载，必须显式报错。
- 不允许在 `rl` 模式出错时静默回退到 `vrptw`，避免用户误判实验结果。

## 6. 实验与训练约束

- v1 只覆盖当前默认实验链路中的单 UAV 调度。
- RL 训练可以新增离线环境、数据集、专家数据导出和训练脚本。
- 训练与评测脚本应尽量复用当前 VRPTW 的时间代价与时间窗语义，保证 apples-to-apples 对比。
- RL 的任何新增实验脚本都不得反向要求修改 UGV 侧核心算法。

## 7. 后续开发原则

- 所有 RL 调度相关代码优先新增，不优先重构原工程其他模块。
- 若必须改 launch 或主入口，只允许做“新增参数、默认行为不变”的最小改动。
- 所有说明文档都应明确指出：RL 目前仅替换 UAV 调度模块，其余链路保持原实现。
